{
  "kind": "collectionType",
  "collectionName": "containers",
  "info": {
    "singularName": "container",
    "pluralName": "containers",
    "displayName": "Container"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "code": {
      "type": "string",
      "required": true
    },
    "size": {
      "type": "enumeration",
      "enum": ["S", "M", "L"],
      "required": true,
      "default": "M"
    },
    "city": {
      "type": "enumeration",
      "enum": ["Dusseldorf", "Koeln"],
      "required": true,
      "default": "Dusseldorf"
    },
    "has_camera": {
      "type": "boolean",
      "default": false
    },
    "internal_camera_id": {
      "type": "string"
    },
    "availability_status": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "available",
        "reserved",
        "rented"
      ]
    },
    "reservation_requests": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::reservation-request.reservation-request",
      "mappedBy": "container"
    }
  }
}
